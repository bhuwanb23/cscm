import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ModuleProgress = () => {
  return null; // ModuleProgress is now integrated into the main Analysis component
};

export default ModuleProgress;

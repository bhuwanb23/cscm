import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ActionBar = () => {
  return null; // ActionBar is now integrated into the main Analysis component
};

export default ActionBar;
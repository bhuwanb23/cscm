apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: cscm
  labels:
    app: gateway
    component: api-gateway
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: gateway
    component: api-gateway
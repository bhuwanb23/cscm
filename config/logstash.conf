input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  # Add any custom filters here
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "cscm-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}